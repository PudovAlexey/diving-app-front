import { useTheme } from "@emotion/react";
import { Typography } from "@mui/material";
import React from "react";

function Logo(props) {
  return (
   <Typography variant="h1">DIVING</Typography>
  );
}

export { Logo };
